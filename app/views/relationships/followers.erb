<div class="col-md-8 offset-md-1">
  <table class="table">
    <h1>FllowerUser</h1>
     <% if  @followers.empty? %>
         <span>ユーザーはいません</span>
      <% else%>
    <thead>
      <tr>
        <th>image</th>
        <th>name</th>
        <th colspan="3"></th>
      </tr>
    </thead>
    <tboy>


    <div class="col-md-8 offset-md-1">
      <% @followers.each do |user| %>
        <tr>
          <th><%= attachment_image_tag user, :profile_image, :fill, 60, 60, format: 'jpeg', fallback: "no_image.jpg" %></th>
          <th>  <%= user.name %> </td>
          <th><%= "フォロー数: #{user.follower.count}" %></th>
          <th><%= "フォロワー数: #{user.followed.count}" %></th>
          <th><%= link_to "Show", user_path(user.id) %></th>
        </tr>
    </div>
    <% end %>
    <% end %>
  </table>
</div>